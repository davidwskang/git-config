# ~/.gitconfig

[alias]
    cb = checkout - b
    co = checkout
    cm = commit -m
    st = status
    br = branch
    squash = rebase -i HEAD~2
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
